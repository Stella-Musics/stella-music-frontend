export type MusicTabType = "일반" | "차트";

export const allMusicTabType: MusicTabType[] = ["일반", "차트"];

export type MusicSortType = "업로드순" | "오래된순" | "조회수순";

export const allMusicSortType: MusicSortType[] = [
  "업로드순",
  "오래된순",
  "조회수순",
];

export type ChartSortType = "시간" | "일간" | "주간" | "월간" | "누적";

export const allChartSortType: ChartSortType[] = [
  "시간",
  "일간",
  "주간",
  "월간",
  "누적"
]
